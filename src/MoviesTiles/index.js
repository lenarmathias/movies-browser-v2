import { MoviesTilesWrapper, MovieTile } from "./styled";

const MoviesTiles = () => {
    return (
        <MoviesTilesWrapper>
            {/* place to implement .map with api */}
            <MovieTile>

            </MovieTile>
        </MoviesTilesWrapper>
    )
};

export default MoviesTiles;